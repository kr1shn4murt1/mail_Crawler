#Fecha: Septiembre 12 - 2012
# Archivo de configuración para usar en conjunto con Estado_Carpetas_Hotmail.py
[setup]
# Quota percentage above which a notification will be sent
breach_level=90

# Parameters of the account to check the quota for
servidor_Pop=pop3.live.com

# Login with IMAPS
use_ssl=yes

# output on console?
verbose=yes

# Email address to send notifications to
send_address=user@domain.tld

# SMTP Parameters
smtp_host=smtp.host.tld
smtp_authentication = 0  # set to 1 if your SMTP server requires authentication, 0 if not
# Following used only when authentication is set to 1
smtp_user=username
smtp_pass=password

[accounts]
pruebas_23@hotmail.com:password
cuenta2@hotmail.com:password2
cuentadeprueba3@hotmail.com:password3
